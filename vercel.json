{
  "buildCommand": "PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK=1 npx prisma migrate deploy && npm run build",
  "crons": [
    {
      "path": "/api/notifications/evaluate",
      "schedule": "0 1 * * *"
    }
  ]
}
