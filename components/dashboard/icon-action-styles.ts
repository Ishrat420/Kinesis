/**
 * The icon-only, no-label shape every small row action shares -- Edit and
 * Dismiss above all, which always appear paired -- so a plain Link and a
 * form's submit button read as one matched set. Each caller adds only its
 * own hover colour on top.
 */
export const ICON_ACTION_CLASS = "flex h-8 w-8 shrink-0 items-center justify-center rounded-lg text-zinc-400 transition";
